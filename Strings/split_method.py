# split() used to split the string using whitespace or given char.
# we can also pass second aurgument as a no. of splits.
# returns the list of char./letters

str = "abc//defghije//klm"

print(str.split("//")) # str.split(char. , no. of splits)