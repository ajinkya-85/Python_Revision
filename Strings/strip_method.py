# strip use to remove whitespaces from the string  (both sides)
# lstrip removes left sides whitespace
# rstrip removes right sides whitespace
# we can also specify the char. to strip from the string only if there are no whitespaces in the str.

str = "ajinkya"
print(str.strip("aji"))
print(str.strip())